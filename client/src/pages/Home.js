import React from "react";
import GameBoard from "../components/GameBoard";
import Hand from '../components/Hand';

const Home = () => {
  return (
    <main>
      <GameBoard />
      <Hand />
    </main>
  );
};

export default Home;
